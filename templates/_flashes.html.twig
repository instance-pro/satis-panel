{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="{{ label == 'error' ? 'flash-error' : (label == 'warning' ? 'flash-warning' : 'flash-success') }}" role="alert">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}
